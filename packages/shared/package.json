{
  "name": "@slack-time-punch/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Slack Time Punch App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --max-warnings 0",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write \"src/**/*.{ts,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,json}\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
